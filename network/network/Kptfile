apiVersion: kpt.dev/v1alpha1
kind: Kptfile
metadata:
  name: network-package
openAPI:
  definitions:
    io.k8s.cli.setters.network-name:
      description: Network name
      x-k8s-cli:
        setter:
          name: network-name
          value: network-name
    io.k8s.cli.setters.network-namespace:
      description: Namespace configured for managing resources defined in this package
      x-k8s-cli:
        setter:
          name: network-namespace
          value: network-namespace
    io.k8s.cli.setters.region:
      description: Region to deploy resources
      x-k8s-cli:
        setter:
          name: region
          value: us-central1
    io.k8s.cli.setters.vpn-secret-name:
      description: VPN secret name
      x-k8s-cli:
        setter:
          name: vpn-secret-name
          value: vpn-shared-secret
    io.k8s.cli.setters.vpn-secret-key:
      description: VPN secret key
      x-k8s-cli:
        setter:
          name: vpn-secret-key
          value: vpn-shared-secret
    io.k8s.cli.substitutions.network-router-name:
      x-k8s-cli:
        substitution:
          name: network-router-name
          pattern: ${NETWORK_NAME}-router
          values:
          - marker: ${NETWORK_NAME}
            ref: '#/definitions/io.k8s.cli.setters.network-name'
    io.k8s.cli.substitutions.network-nat-name:
      x-k8s-cli:
        substitution:
          name: network-nat-name
          pattern: ${NETWORK_NAME}-router-nat
          values:
          - marker: ${NETWORK_NAME}
            ref: '#/definitions/io.k8s.cli.setters.network-name'
    io.k8s.cli.substitutions.vpn-gateway-name:
      x-k8s-cli:
        substitution:
          name: network-gateway-name
          pattern: ${NETWORK_NAME}-vpn-gateway
          values:
          - marker: ${NETWORK_NAME}
            ref: '#/definitions/io.k8s.cli.setters.network-name'
    io.k8s.cli.substitutions.vpn-tunnel-name:
      x-k8s-cli:
        substitution:
          name: vpn-tunnel-name
          pattern: ${NETWORK_NAME}-vpn-tunnel
          values:
          - marker: ${NETWORK_NAME}
            ref: '#/definitions/io.k8s.cli.setters.network-name'
    io.k8s.cli.substitutions.vpn-address-name:
      x-k8s-cli:
        substitution:
          name: vpn-address-name
          pattern: ${NETWORK_NAME}-vpn-address
          values:
          - marker: ${NETWORK_NAME}
            ref: '#/definitions/io.k8s.cli.setters.network-name'
    io.k8s.cli.substitutions.vpn-fr-name:
      x-k8s-cli:
        substitution:
          name: vpn-fr-name
          pattern: ${NETWORK_NAME}-vpn-fr
          values:
          - marker: ${NETWORK_NAME}
            ref: '#/definitions/io.k8s.cli.setters.network-name'
    io.k8s.cli.substitutions.subnetwork-name:
      x-k8s-cli:
        substitution:
          name: subnetwork-name
          pattern: ${NETWORK_NAME}-subnetwork
          values:
          - marker: ${NETWORK_NAME}
            ref: '#/definitions/io.k8s.cli.setters.network-name'
