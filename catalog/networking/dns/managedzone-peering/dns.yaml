apiVersion: dns.cnrm.cloud.google.com/v1beta1
kind: DNSManagedZone
metadata:
  annotations:
    cnrm.cloud.google.com/blueprint: cnrm/dns-peered-managed-zone/v0.2.0-alpha.1
    cnrm.cloud.google.com/project-id: project-id # {"$kpt-set":"project-id"}
  name: dnsmanagedzone-sample # {"$kpt-set":"managed-zone-name"}
  namespace: networking # {"$kpt-set":"namespace"}
spec:
  dnsName: example.com. # {"$kpt-set":"domain"}
  peeringConfig:
    targetNetwork:
      networkRef:
        name: example-peered-network # {"$kpt-set":"peered-network-name"}
        namespace: networking # {"$kpt-set":"namespace"}
  privateVisibilityConfig:
    networks:
    - networkRef:
        name: example-network
        namespace: networking
  visibility: private
