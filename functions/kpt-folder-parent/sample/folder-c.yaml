apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
kind: Folder
metadata:
  annotations:
    cnrm.cloud.google.com/folder-ref: |
      {"namespace": "one", "name": "top-folder"}
    config.kubernetes.io/function: |
      container:
        image: gcr.io/yakima-eap/folder-ref:latest
  name: third-folder
  namespace: two
spec:
  displayName: third-folder
