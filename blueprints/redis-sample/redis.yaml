apiVersion: serviceusage.cnrm.cloud.google.com/v1beta1
kind: Service
metadata:
  annotations:
    cnrm.cloud.google.com/disable-on-destroy: "false"
    cnrm.cloud.google.com/project-id: project-id # {"$kpt-set":"project-id"}
  name: project-id-redis # {"$kpt-set":"services-redis"}
  namespace: my-namespace # {"$ref":"#/definitions/io.k8s.cli.setters.namespace"}
spec:
  resourceID: redis.googleapis.com
---
apiVersion: redis.cnrm.cloud.google.com/v1beta1
kind: RedisInstance
metadata:
  name: redis-sample-001 # {"$ref":"#/definitions/io.k8s.cli.setters.name"}
  namespace: my-namespace # {"$ref":"#/definitions/io.k8s.cli.setters.namespace"}
spec:
  authorizedNetworkRef:
    external: projects/my-project/global/networks/default # {"$ref":"#/definitions/io.k8s.cli.substitutions.networkref"}
  displayName: redis-sample-001 # {"$ref":"#/definitions/io.k8s.cli.setters.name"}
  memorySizeGb: 5
  region: us-central1 # {"$ref":"#/definitions/io.k8s.cli.setters.region"}
  tier: BASIC
