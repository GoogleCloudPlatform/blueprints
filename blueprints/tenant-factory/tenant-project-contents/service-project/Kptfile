apiVersion: kpt.dev/v1alpha1
kind: Kptfile
metadata:
  name: service-project
packageMetadata:
  shortDescription: Allow the tenant project to use the Shared VPC
openAPI:
  definitions:
    io.k8s.cli.setters.tenant-project-id:
      x-k8s-cli:
        setter:
          name: tenant-project-id
          value: tenant-dev-div1-envb
    io.k8s.cli.setters.network-project-id:
      x-k8s-cli:
        setter:
          name: network-project-id
          value: network-project-id
