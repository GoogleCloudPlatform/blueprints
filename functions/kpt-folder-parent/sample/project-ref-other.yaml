# This project references a parent not defined in this sample. 
apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
kind: Project
metadata:
  annotations:
    cnrm.cloud.google.com/folder-ref: |
      {"namespace": "other-namespace", "name": "outer-folder"}
    config.kubernetes.io/function: |
      container:
        image: gcr.io/yakima-eap/folder-ref:latest
  name: project-ref-other
  namespace: one
spec:
  name: project-ref-other
