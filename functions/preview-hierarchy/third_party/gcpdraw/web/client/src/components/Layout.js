import React from 'react';

export const PageTitleBar = ({children}) => (
  <div className='page-title-bar'>{children}</div>
);

export const PageTitle = ({children}) => (
  <h1 className='page-title'>{children}</h1>
);
