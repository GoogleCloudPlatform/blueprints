apiVersion: kpt.dev/v1alpha2
kind: Kptfile
metadata:
  name: dns-policy
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/apply-setters:v0.1
      configMap:
        dns-policy-name: default-dns-policy
        namespace: networking
        network-name: example-network
        project-id: project-id
